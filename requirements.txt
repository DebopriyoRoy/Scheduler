# Runtime and development dependencies, pinned so a fresh install on another Mac
# resolves to exactly the versions this app was built and tested against.
#
# Playwright is a hard runtime requirement, not a dev extra: the live show-calendar
# and Square availability sync both drive a headless Chromium through it. It was
# previously missing from this file, so a clean install produced an app that started
# fine and then failed at the first calendar sync.
#
# After installing these, fetch the browser binary too - it lives outside the venv:
#     python -m playwright install chromium

Django==6.0.8
requests==2.34.2
python-dotenv==1.2.3
beautifulsoup4==4.15.0
playwright==1.62.0

# Serves static files from Django itself; the desktop install has no web server
# in front of it, and without this the interface loads unstyled when DEBUG is off.
whitenoise==6.11.0

# Exports
openpyxl==3.1.5
reportlab==4.5.1
pillow==12.3.0

# PostgreSQL driver. Not needed for the standard single-machine SQLite install;
# kept because settings.database_config() supports a postgres DATABASE_URL.
psycopg[binary]==3.3.4

# Development and CI only
pytest==9.1.1
pytest-django==4.14.0
ruff==0.16.4
